/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["https://gomoonbeam.com", "https://cursor.so", "https://userogue.com", "https://editorially.org", "https://editrix.ai", "aceternity.com"]
    }
};

export default nextConfig;
